# 行尾一律 LF 存进仓库，不受各人 core.autocrlf 影响。
# 不加这条的话：Windows 上 core.autocrlf=true 的人提交的是 LF、false 的人提交的是 CRLF，
# 同一个文件在历史里出现两种行尾，diff 全是噪声。
* text=auto eol=lf

# 明确按二进制处理，别做任何行尾转换
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
